# Animation Plans — Landing Page

Generated by the `improve-animations` skill on 2026-09-01 against commit
`68fe3d5`. Scope: `src/pages/Landing/Landing.tsx` + `Landing.module.css`.

The skill is read-only on source code. To execute a plan, hand it to any agent
(including a cheaper model) — every plan is self-contained with exact file
paths, current code excerpts, target values, and verification steps.

## Status legend

| Status | Meaning |
| --- | --- |
| `TODO` | Plan written, not yet executed |
| `IN PROGRESS` | Executor is currently applying the plan |
| `DONE` | Plan applied and merged |
| `STALE` | Source drift since plan stamp — re-check before executing |

## Plan table

| # | Title | Severity | Status | Files |
| --- | --- | --- | --- | --- |
| [001](001-tokenize-landing-transitions.md) | Consolidate landing-page transitions onto project motion tokens | MEDIUM | DONE | `src/pages/Landing/Landing.module.css` |
| [002](002-workspace-hover-compositor-only.md) | Split workspace-card hover into transform-only motion | MEDIUM | DONE | `src/pages/Landing/Landing.module.css` |
| [003](003-workspace-card-cascade.md) | Add one-shot staggered cascade to workspace cards | MEDIUM | DONE | `src/pages/Landing/Landing.tsx`, `Landing.module.css` |
| [004](004-hero-entrance.md) | Add one-shot hero entrance | LOW | DONE | `src/pages/Landing/Landing.tsx`, `Landing.module.css` |
| [005](005-primary-cta-press-feedback.md) | Add press feedback to primary CTA + login button | MEDIUM | DONE | `src/pages/Landing/Landing.module.css` |

Applied in the recommended order: 001 → 005 → 002 → 003 → 004. `npx tsc --noEmit`
passes (exit 0). `npx vite build` passes (exit 0). No ESLint config exists in
this project, so the lint step is unapplicable.

## Recommended execution order

1. **001** first. Token swap is mechanical and unblocks plan 005 (which
   relies on `var(--transition-fast)` being wired to the buttons).
2. **005** second. Quick win — press feedback on the two primary CTAs.
3. **002** third. Improves the workspace card hover perf.
4. **003** fourth. Adds the entrance cascade to the workspace cards.
5. **004** fifth. Adds the hero entrance last, after the cascade pattern
   from 003 has been applied (so both use the same keyframe + custom-property
   approach).

## Notes

- All plans respect `prefers-reduced-motion` either via the project's
  global token system (`src/styles/ars-tokens.css:360-368`) or via explicit
  per-rule overrides.
- All plans animate `transform` and `opacity` only — no layout-affecting
  properties. This keeps motion on the compositor layer.
- The skill's "audit" found 11 total findings; this index covers the top 5
  by leverage (impact ÷ effort). Remaining findings (FAQ `<details>`
  crossfade, kicker underline reveal, publication-flow stagger, etc.) can
  become plans if you want a deeper pass.