import styles from './PublicationShared.module.css';

export const PublicationDemoBanner = () => (
  <div className={styles.demoBanner} role="status">
    Demo catalog data - awaiting backend published-paper API
  </div>
);
